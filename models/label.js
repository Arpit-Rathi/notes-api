const { db } = require('../utils/database');

const mongodb = require('mongodb');

class Label {
    constructor() {
        this.userId = userId;
        this.title = title;
        this.color = color;
    }
}

module.exports = Label;